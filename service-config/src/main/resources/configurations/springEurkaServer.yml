eureka:
  instance:
    hostname: springEurkaServer
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
    registerWithEureka: false
    fetch-registry: false